import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.libaton',
  appName: 'Libaton',
  webDir: 'dist/picolo2.0',
  bundledWebRuntime: false
};

export default config;
